cohort <- patientDataTable
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1,3)
patientDataTable <- read.csv(file="./Cohort.csv", header=TRUE, sep=",")
patientDataTable <- read.csv(file="../Cohort.csv", header=TRUE, sep=",")
patientDataTable <- read.csv(file="/Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1,3)
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1,3)
setwd("~/Coding/Ginza")
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1,3)
patientDataTable
cohort <- patientDataTable
sel <-vectorIndexToApply
cohort
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1,3)
# TEST DATA END
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
filteredDataTable <- cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
sel
filteredDataTable
nrow(filteredDataTable)
calculatePatientCount(read.csv(file="Cohort.csv", header=TRUE, sep=","), c(1,3))
shiny::runApp()
runApp()
runApp()
runApp()
nrow(filteredDataTable)
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1)
## TEST DATA END
if (!is.null(patientDataTable)
&& !is.null(vectorIndexToApply)
&& length(vectorIndexToApply) != 0
&& nrow(patientDataTable) != 0) {
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
filteredDataTable <- cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
return(nrow(filteredDataTable))
}
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1)
## TEST DATA END
if (!is.null(patientDataTable)
&& !is.null(vectorIndexToApply)
&& length(vectorIndexToApply) != 0
&& nrow(patientDataTable) != 0) {
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
filteredDataTable <- cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
}
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1)
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
filteredDataTable <- cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
return(nrow(filteredDataTable))
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
filteredDataTable <- cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1)
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
filteredDataTable <- cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
cohort <- patientDataTable
sel <-vectorIndexToApply
cohort
sel
sel
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1, 2)
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
cols <- sapply(cohortsel, is.logical)
cohortsel[,cols] <- lapply(cohortsel[,cols], as.numeric)
filteredDataTable <- cohortsel[apply(cohortsel, MARGIN = 1, function(x) all(x==1)), ]
nrow(filteredDataTable)
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
vectorIndexToApply <- c(1)
cohort <- patientDataTable
sel <-vectorIndexToApply
cohortsel<-cohort[,sel]
for (i in 1:length(sel)) {
if (substr(names(cohortsel)[i],1,2)=='Ex') {cohortsel[,i]<-!cohortsel[,i]}
}
runApp()
runApp()
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1)
for (i in 1:length(patientDataTable)) {
if (substr(names(patientDataTable)[i], 1, 2) == 'Ex') {
patientDataTable[,i] <- !patientDataTable[,i]
}
}
patientDataTable
for (i in 1:length(patientDataTable)) {
if (substr(names(patientDataTable)[i], 1, 2) == 'Ex') {
patientDataTable[,i] <- abs(patientDataTable[,i] - 1)
}
}
patientDataTable
appliedPatientDataTable<-cohort[,vectorIndexToApply]
appliedPatientDataTable
appliedPatientDataTable<-patientDataTable[,vectorIndexToApply]
appliedPatientDataTable
for (i in 1:length(patientDataTable)) {
if (substr(names(patientDataTable)[i], 1, 2) == 'Ex') {
patientDataTable[,i] <- abs(patientDataTable[,i] - 1)
}
}
appliedPatientDataTable<-patientDataTable[,vectorIndexToApply]
cols <- sapply(appliedPatientDataTable, is.logical)
appliedPatientDataTable[,cols] <- lapply(appliedPatientDataTable[,cols], as.numeric)
filteredDataTable <- appliedPatientDataTable[apply(appliedPatientDataTable, MARGIN = 1, function(x) all(x==1)), ]
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1)
for (i in 1:length(patientDataTable)) {
if (substr(names(patientDataTable)[i], 1, 2) == 'Ex') {
patientDataTable[,i] <- abs(patientDataTable[,i] - 1)
}
}
appliedPatientDataTable<-patientDataTable[,vectorIndexToApply]
cols <- sapply(appliedPatientDataTable, is.logical)
appliedPatientDataTable[,cols] <- lapply(appliedPatientDataTable[,cols], as.numeric)
appliedPatientDataTable<-patientDataTable[,vectorIndexToApply]
cols <- sapply(appliedPatientDataTable, is.logical)
appliedPatientDataTable<-patientDataTable[,vectorIndexToApply]
appliedPatientDataTable
filteredDataTable <- appliedPatientDataTable[apply(appliedPatientDataTable, MARGIN = 1, function(x) all(x==1)), ]
appliedPatientDataTable<-patientDataTable[,vectorIndexToApply]
appliedPatientDataTable
runApp()
patientDataTable <- read.csv(file="Cohort.csv", header=TRUE, sep=",")
vectorIndexToApply <- c(1)
for (i in 1:length(patientDataTable)) {
if (substr(names(patientDataTable)[i], 1, 2) == 'Ex') {
patientDataTable[,i] <- abs(patientDataTable[,i] - 1)
}
}
appliedPatientDataTable<-as.data.frame(patientDataTable[,vectorIndexToApply])
appliedPatientDataTable
filteredDataTable <- appliedPatientDataTable[apply(appliedPatientDataTable, MARGIN = 1, function(x) all(x==1)), ]
nrow(filteredDataTable)
filteredDataTable <- appliedPatientDataTable[apply(appliedPatientDataTable, MARGIN = 1, function(x) all(x==1)), ]
filteredDataTable
filteredDataTable <- as.data.frame(appliedPatientDataTable[apply(appliedPatientDataTable, MARGIN = 1, function(x) all(x==1)), ])
filteredDataTable
nrow(filteredDataTable)
vectorIndexToApply <- c(1, 2)
for (i in 1:length(patientDataTable)) {
if (substr(names(patientDataTable)[i], 1, 2) == 'Ex') {
patientDataTable[,i] <- abs(patientDataTable[,i] - 1)
}
}
appliedPatientDataTable<-as.data.frame(patientDataTable[,vectorIndexToApply])
filteredDataTable <- as.data.frame(appliedPatientDataTable[apply(appliedPatientDataTable, MARGIN = 1, function(x) all(x==1)), ])
nrow(filteredDataTable)
runApp()
